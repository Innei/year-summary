export default {
  options: state => state.options,
  year: state => state.options.year,
  avatar: state => state.options.avatar,
  theme: state => state.options.theme,
  viewport: state => state.viewport.viewport
}
